# Python
__pycache__/
*.pyc

# Сборка PyInstaller (.exe собирается в CI/локально, в репозиторий не кладём)
build/
dist/
*.spec

# Секреты — в план не пишутся (там только имя env-переменной), но на всякий случай
.env

# Атомарная замена планов оставляет .tmp только при сбое записи
projects/*.tmp

# OS
.DS_Store
Thumbs.db
